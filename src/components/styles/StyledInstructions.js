import styled from 'styled-components';
import { LIGHT_GRAY } from '../../constants/AppColours';

export default styled.p`
  align-self: center;
  text-align: center;
  justify-self: center;
  color: ${LIGHT_GRAY};
  font-size: 1rem;
`;