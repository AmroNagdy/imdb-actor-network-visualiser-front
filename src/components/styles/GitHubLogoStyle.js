import styled from 'styled-components';

const GitHubLogoStyle = styled.img`
  filter: invert(1);
  height: auto; 
  width: auto; 
  max-width: 50px; 
  max-height: 50px;
  padding-top: 10px;
  padding-bottom: 10px;
`;

export default GitHubLogoStyle;